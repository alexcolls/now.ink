# now.ink Production Environment Configuration
# Copy to .env.production and fill in actual values

# Database
DB_PASSWORD=CHANGE_ME_strong_password_here

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=CHANGE_ME_generate_secure_jwt_secret

# Solana Configuration
SOLANA_NETWORK=mainnet-beta
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com
PLATFORM_WALLET_PRIVATE_KEY=CHANGE_ME_your_solana_wallet_private_key

# Minting (set to true for production)
USE_REAL_MINTING=true

# Arweave
ARWEAVE_WALLET_PATH=./arweave-keyfile.json

# CORS (replace with your actual domains)
CORS_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Optional: Custom Solana RPC (for better performance)
# SOLANA_RPC_URL=https://your-custom-rpc-endpoint.com
